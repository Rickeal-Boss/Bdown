# Bdown 隐私政策

**生效日期**：2026-09-19
**适用版本**：v1.4.0

---

## 一句话总结

Bdown **不收集、不上传、不共享任何用户数据**。所有功能都在你的浏览器本地完成。

---

## 我们收集什么

**什么都不收集。**

- 没有账号系统，不需要注册或登录
- 没有任何统计、埋点、崩溃上报
- 没有远程服务器，没有任何数据离开你的设备
- 没有广告，没有第三方 SDK

---

## 我们存储什么（全部在本地）

扩展只使用浏览器自带的 `chrome.storage.local`，数据**仅保存在本机**，不会同步到任何云端：

| 数据 | 用途 | 何时清除 |
|---|---|---|
| 下载设置（清晰度、保存路径、文件名模板、并发数等） | 记住你的偏好 | 在选项页重置，或卸载扩展 |
| 待下载任务列表 | 弹窗派发任务给下载中心 | 下载中心读取后立即清除 |
| 下载历史（最多 200 条，含视频标题 / UP 主名 / BV 号） | 下载中心显示历史记录 | 在下载中心点「清除」按钮，或卸载扩展 |
| 断点续传进度（**默认关闭**） | 开启后记录已下载的字节区间 | 任务完成后自动清除；7 天未更新自动失效 |

**我们不存储**：
- ❌ B 站登录 Cookie（`SESSDATA` / `bili_jct` / `DedeUserID` 等）—— 扩展**未申请** `cookies` 权限，代码也无法读取它们
- ❌ 你的 B 站账号、密码、手机号
- ❌ 浏览历史、书签

---

## 权限说明（为什么需要）

| 权限 | 用途 |
|---|---|
| `storage` / `unlimitedStorage` | 保存设置与下载历史（上表） |
| `downloads` | 把合并好的视频保存到你的下载目录 |
| `tabs` | 读取**当前**标签页的 URL，以识别你正在看的视频 |
| `contextMenus` | 右键菜单的「用 Bdown 下载」 |
| `declarativeNetRequestWithHostAccess` | 给发往 B 站 CDN 的请求补 `Referer`/`User-Agent`；给 `api.bilibili.com` 的请求补 `Origin`。这是 B 站接口的要求，否则会返回错误 |
| `host_permissions`（bilibili / bilivideo / hdslb / akamaized / biliapi） | 访问 B 站的视频信息与媒体地址 |

**已刻意移除**的权限：`scripting`、`notifications`、`cookies` —— 代码中 0 处引用，不需要就不申请。
**已移除** `web_accessible_resources` —— 不向网页暴露任何扩展内部资源。

---

## 登录与高清

Bdown **不使用自己的账号系统**。你在浏览器里登录 B 站后，扩展发出的请求会**由浏览器自动携带** B 站的 Cookie（`credentials: 'include'`），从而拿到你账号权限内的清晰度（大会员可到 4K/HDR/1080P+，非会员最高 1080P）。

**扩展不读取、不保存、不传输这些 Cookie** —— 它们只存在于浏览器的 Cookie 管理器里。

---

## 网络安全

- 所有请求均为 HTTPS；下载链接若返回 `http://`，扩展会**强制升级为 `https://`**
- 不向任何第三方发送数据

---

## 数据删除

- 卸载扩展 = 删除全部本地数据
- 选项页可重置设置
- 下载中心可清除历史记录

---

## 联系

本项目开源于 GitHub（`Rickeal-Boss/Bdown`）。如有隐私相关问题，请在仓库提交 Issue。

---

## 变更记录

- **2026-09-19**（v1.4.0）：首次发布隐私政策；补充断点续传（默认关闭）的存储说明与 7 天失效规则。
